# Cosa serve alla scansione, e nient'altro: questo file viene installato da
# capo a ogni giro, quindi ogni pacchetto di troppo e' tempo speso a vuoto
# quarantotto volte al giorno. Il vecchio server locale aveva le sue, e se le
# e' portate via.
requests>=2.32.3        # HTTP semplice
curl_cffi>=0.7.4        # impronta TLS di un browser vero: Subito e Amazon
beautifulsoup4>=4.13.3  # lettura dell'HTML
lxml>=5.3.0             # il parser veloce sotto BeautifulSoup
pyyaml>=6.0.2           # config/*.yaml
playwright>=1.50.0      # Chromium headless, per NorthLadder
